import GridPagesSkeleton from '../lib/components/skeletons/GridPagesSkeleton';

export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <GridPagesSkeleton />
    )
  }